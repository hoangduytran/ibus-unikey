Comment include/ukengine/engine header enums and structures with code_comments.md style

- Add file-level documentation to `include/ukengine/engine/ukengine.h` and `include/ukengine/engine/unikey.h`.
- Add detailed member and method comments to `UkSharedMem`, `UkEngine`, `KeyBufEntry`, and `VnWordForm`.
- Add per-value documentation for `VnWordForm` enum elements.
- Add per-field documentation for `WordInfo` struct fields.
- Add API docs for `SetupUnikeyEngine`.

Reasoning:
- Improve maintainability and readability for engine integration interfaces.
- Surface intent, state lifecycle, and mode behavior in public API headers.
- Follow code comment guidelines in `.github/skills/development-guidelines/code_comments.md`.

Validation:
- `git diff` shows only comment and doc changes in include/ukengine/engine headers.
- `./mkme` still succeeds with no behavioral changes.
- `ctest --output-on-failure` still passes.

