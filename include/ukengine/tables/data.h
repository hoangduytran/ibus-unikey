// -*- coding:unix; mode:c++; tab-width:4; c-basic-offset:4; indent-tabs-mode:nil -*-
#ifndef VIETNAMESE_CHARSET_DATA_H
#define VIETNAMESE_CHARSET_DATA_H

/* Legacy name: `StdStartQuote`, `StdEndQuote`, and `StdEllipsis` are defined in
 * `mapping/charset.h` (after `VnStdCharOffset`). This header is kept for old
 * `#include "data.h"` from the tables layer. */

#endif
